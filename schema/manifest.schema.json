{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["pipeline", "version", "timestamp", "config", "aggregate_metrics"],
  "properties": {
    "pipeline": {"type": "string"},
    "version": {"type": "string"},
    "timestamp": {"type": "string"},
    "config": {"type": "object"},
    "aggregate_metrics": {"type": "object"}
  }
}
